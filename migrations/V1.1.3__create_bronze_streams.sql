-- =========================================================
-- V1.1.3 - Streams on bronze tables (CDC for silver merges)
-- =========================================================

CREATE STREAM IF NOT EXISTS BRONZE_LAYER.CUSTOMER_RAW_STREAM
    ON TABLE BRONZE_LAYER.CUSTOMER_RAW;

CREATE STREAM IF NOT EXISTS BRONZE_LAYER.GEOLOCATION_RAW_STREAM
    ON TABLE BRONZE_LAYER.GEOLOCATION_RAW;

CREATE STREAM IF NOT EXISTS BRONZE_LAYER.SELLERS_RAW_STREAM
    ON TABLE BRONZE_LAYER.SELLERS_RAW;

CREATE STREAM IF NOT EXISTS BRONZE_LAYER.PRODUCTS_RAW_STREAM
    ON TABLE BRONZE_LAYER.PRODUCTS_RAW;

CREATE STREAM IF NOT EXISTS BRONZE_LAYER.PRODUCT_CATEGORY_TRANSLATION_RAW_STREAM
    ON TABLE BRONZE_LAYER.PRODUCT_CATEGORY_TRANSLATION_RAW;

CREATE STREAM IF NOT EXISTS BRONZE_LAYER.STREAM_ORDERS
    ON TABLE BRONZE_LAYER.ORDERS_RAW
    APPEND_ONLY = TRUE;